# Go
/bin/
/favro-mcp
/favro-mcp.exe
*.exe
*.test
*.out
/dist/

# Environment / secrets (never commit)
.env
.env.local
.mcp.json

# Local MCP config (may contain credentials)
.mcp.json

# Raw API recordings (contain REAL PII/credentials). Capture with
# FAVRO_RECORD_DIR, then anonymize into testdata/fixtures/ BEFORE committing.
# Never commit the raw directory.
/recordings/
/testdata/recordings/
/testdata/recordings-*/

# Dev-only capture/exploration programs (exercised against the live API).
/dev/

